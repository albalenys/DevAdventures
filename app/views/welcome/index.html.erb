<h1 class="center">Developing Adventures</h1>
<div class="subtitle">from a Beginner Developer</div>

<div class="index_text">
  *Most blog posts have been taken down temporarily. They will be readded during the next few days.
  <h3>Recent Work</h3>
  <% @recent_work.each do |work|%>
    <%= work.created_at.strftime("%B, %d, %Y") %><br>
    <ul>
      <li><small><%= work.class %></small>: <%= link_to "#{work.title}", post_path(work) %></li>
    </ul>
  <% end %>
</div>
