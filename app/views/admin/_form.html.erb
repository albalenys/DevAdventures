<%= flash[:error] %>

<%= form_for @admin, url: {action: "login"} do |admin_form| %>
  Username: <%= admin_form.text_field :username %><br>
  Password: <%= admin_form.password_field :password %><br>
  <%= admin_form.submit "Submit", class: "sub-button" %>
<% end %>
