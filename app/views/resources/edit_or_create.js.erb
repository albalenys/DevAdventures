if (@status == "new") {
  let grabber = "#resource-list";
  alteredHtml = "<%= escape_javascript render 'resources/new' %>";
} 
else {
  let grabber = "#resource-<%= @resource.id %>";
  alteredHtml = "<%= escape_javascript render 'resources/edit' %>";
}

$(grabber).append(alteredHtml);