<span class="center">
  <%= flash[:error] %>
</span>

<%= form_for [@post, @tag] do |f|%>
  <% f.hidden_field :post_id, value: @post.id %>
  <%= f.text_field :name, placeholder: "Name" %>
  <%= f.submit "+" %>
<% end %>